{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "workspaces": [
    "artifacts/*",
    "lib/*",
    "lib/integrations/*",
    "scripts"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=artifacts/study-tracker",
    "dev:study-tracker": "npm run dev --workspace=artifacts/study-tracker",
    "dev:mockup": "npm run dev --workspace=artifacts/mockup-sandbox",
    "build": "npm run build --workspace=artifacts/study-tracker",
    "typecheck": "tsc --build",
    "lint": "tsc --build"
  },
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1",
    "firebase": "^12.16.0"
  },
  "devDependencies": {
    "prettier": "^3.9.5",
    "typescript": "~5.9.3"
  }
}